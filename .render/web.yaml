services:
  - type: web
    name: websocket-server
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "./start.sh"
    autoDeploy: false
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
    plan: free
